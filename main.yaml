---
- name: Configure DNS
  hosts:
    - cephadm-nodes
    - ceph-ansible-nodes
  remote_user: ubunutu
  tasks:
    - name: Adding Shecan DNS Nameserver to /etc/resolv.conf
      include: ./dns.yaml

# --------------------------------------------------------

- name: Install Docker
  hosts:
    - cephadm-nodes
  remote_user: ubunutu
  tasks:
    - name: Install Docker on Cephadm Nodes
      include: ./docker.yaml

  handlers:
    - name: restart docker
      service:
        name: docker
        state: restarted

...